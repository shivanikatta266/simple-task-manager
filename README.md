# simple-task-manager
A simple web-based Task Manager application built using HTML, CSS, and JavaScript. It allows users to create, view, edit, and delete tasks, organize them by status (To Do, In Progress, Done), and manage task data using browser localStorage. The UI is implemented based on the provided mock-up and runs locally without any backend setup.
